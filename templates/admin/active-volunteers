{% extends "admin/base.html" %}
{% block title %}Действующие волонтёры — DUO Charity Admin{% endblock %}

{% block content %}
    <div class="page-head">
        <div>
            <h1 class="page-title">Действующие волонтёры</h1>
            <p class="page-subtitle">{{ volunteers|length }} волонтёров в базе</p>
        </div>
        <a href="{{ url_for('admin_volunteers_export') }}" class="btn btn-outline">Скачать Excel</a>
    </div>

    {% if volunteers %}
    <div class="admin-table-wrapper">
        <table class="admin-table admin-table-responsive">
            <thead>
                <tr>
                    <th>Имя</th>
                    <th>Телефон</th>
                    <th>Авто</th>
                    <th>Бот</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                {% for v in volunteers %}
                <tr>
                    <td data-label="Имя">
                        <strong>{{ v.full_name }}</strong>
                        {% if v.occupation %}<p class="table-excerpt">{{ v.occupation }}</p>{% endif %}
                    </td>
                    <td data-label="Телефон">
                        <a href="tel:{{ v.phone }}">{{ v.phone }}</a>
                    </td>
                    <td data-label="Авто">
                        {% if v.has_car %}
                            <span class="tag tag-primary">{{ v.car_plate or 'номер не указан' }}</span>
                        {% elif v.has_car == false %}
                            <span class="tag">нет</span>
                        {% else %}
                            <span class="tag">—</span>
                        {% endif %}
                    </td>
                    <td data-label="Бот">
                        {% if v.telegram_chat_id %}
                            <span class="status-badge status-badge-closed">подключён</span>
                        {% else %}
                            <span class="status-badge status-badge-new">не подключён</span>
                        {% endif %}
                    </td>
                    <td class="table-actions" data-label="">
                        <form method="post" action="{{ url_for('admin_active_volunteer_delete', volunteer_id=v.id) }}"
                              onsubmit="return confirm('Удалить этого волонтёра?');">
                            <button type="submit" class="btn-small btn-small-danger">Удалить</button>
                        </form>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    {% else %}
    <p class="empty-state-admin">Пока нет ни одного действующего волонтёра. Принимайте заявки на странице «Волонтёры».</p>
    {% endif %}
{% endblock %}